const UserProfile = () => {
  const handler = () => {
  };

  return (
    <form>
      <input type="file" />
      <button onClick={handler}>Chang profile picture</button>
    </form>
  );
};

export default UserProfile;
